<!-- app/views/layouts/application.html.erb -->
<%= stylesheet_link_tag 'application', 'https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/css/bootstrap-datepicker.min.css' %>
  <%= javascript_include_tag 'application', 'https://code.jquery.com/jquery-3.6.0.min.js', 'https://cdnjs.cloudflare.com/ajax/libs/bootstrap-datepicker/1.9.0/js/bootstrap-datepicker.min.js' %>
<%= javascript_include_tag "https://cdn.jsdelivr.net/npm/chartkick@4.0.2"  %>
<%= javascript_include_tag "https://cdn.jsdelivr.net/npm/chart.js@2.9.4"  %>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css">
<%#= hai am manju %>
<!DOCTYPE html>
<!-- app/views/layouts/application.html.erb -->
<% flash.each do |key, value| %>
  <div class="flash <%= key %>" style="color: red;">
    <%= value %>
  </div>
<% end %>
<html>
  <head>
    <!-- Your head content here -->
  </head>
  <body>
    <nav>
      <% if session[:user_id] %>
        <%= link_to 'Home', home_path %>
        <% if User.find(session[:user_id]).roles.exists?(name: 'admin') %>
          <%= link_to 'Admin Dashboard', admin_dashboard_path %>
        <% end %>
        <%= link_to 'Logout', logout_path, method: :delete %>
<% else %>
	    <%= link_to 'Signup', signup_path %>

        <%= link_to 'Login', login_path %>
      <% end %>
    </nav>

    <%= yield %>

    <!-- Your other footer content here -->
  </body>
</html>

